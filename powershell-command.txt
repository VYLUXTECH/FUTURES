MT5 IPC Timeout Fix Guide:

Credentials:
  Login: 49362403
  Password: #RichieRich206
  Server: HFMarketsGlobal-Demo

1. Kill MT5 processes:
   

2. Start MT5 manually and wait for it to load:
   
   (Wait for MT5 window to open fully, log in manually with account 49362403)

3. Once logged in and fully loaded, minimize MT5 and run this to connect to the running instance:
   python -c "import MetaTrader5 as mt5; mt5.initialize(); account=mt5.account_info(); print('='*40); print('  MT5 CONNECTED!' if account else '  FAILED: ' + str(mt5.last_error())); print('='*40); print(f'  Balance: {account.balance} {account.currency}') if account else None; mt5.shutdown()"
